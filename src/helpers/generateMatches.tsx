import { MatchScore, StateModel } from "../types";

const generateMatches = (state: StateModel): MatchScore[] => {

    for(let i in state.teams){
        for(let j in state.teams){
            
        }
    }
        
    return []
}

export default generateMatches;